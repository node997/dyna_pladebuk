# dyna_pladebuk

Project for bending a plate using induction heating.

- `d3plot` files are not included because GitHub cannot handle the large file sizes.
- To reproduce the results, run `main.k` using double precision, or modify the bash script to perform multiple iterations.
